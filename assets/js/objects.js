const filterButtons=document.querySelectorAll("#filter-buttons button"),filterableCards=document.querySelectorAll("#filterable-cards .card"),filterCards=t=>{document.querySelector("#filter-buttons .active").classList.remove("active"),t.target.classList.add("active"),filterableCards.forEach(e=>{if(e.dataset.name===t.target.dataset.filter||"all"===t.target.dataset.filter)return e.classList.replace("hide","show");e.classList.add("hide")})};filterButtons.forEach(t=>t.addEventListener("click",filterCards));