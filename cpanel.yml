---
deployment:
  tasks:
    - export REPOPATH=/home/anzhelikab/repositories/*
    - export SITEPATH=/home/anzhelikab/public_html/
    - /bin/rsync -rav --exclude '.git' --exclude '.cpanel.yml' --del $REPOPATH $SITEPATH                         
