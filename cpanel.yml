---
deployment:
      tasks:
        - export DEPLOYPATH=/home/anzhelikab/public_html  # Add /<app_folder> if required
        - /bin cp * $DEPLOYPATH                           #copy all from root 
        - /bin cp /assets/* $DEPLOYPATH             #copy all from sub folder root
   
        
        
